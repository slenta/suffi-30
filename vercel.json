{
  "buildCommand": "pip install pygbag && cd platformer && python -m pygbag --build --archive .",
  "outputDirectory": "platformer/build/web",
  "installCommand": "echo 'Dependencies installed in buildCommand'",
  "headers": [
    {
      "source": "/(.*)",
      "headers": [
        {
          "key": "Cross-Origin-Embedder-Policy",
          "value": "require-corp"
        },
        {
          "key": "Cross-Origin-Opener-Policy",
          "value": "same-origin"
        }
      ]
    }
  ]
}
