"""API configuration for web builds."""

# Set this to your deployed Vercel API URL when running locally with pygbag
# Example: "https://your-app.vercel.app/api"
# Leave as None to use localStorage fallback when testing locally
API_BASE_URL = "https://suffi-30.vercel.app/api"

# When deployed to Vercel/production, this will be auto-detected
# Only set this manually for local development with pygbag
